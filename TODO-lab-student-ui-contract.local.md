# 学生端 UI 重构契约

本轮范围：按教师端接入 `@ls101/desktop-ui` 与 `@ls101/lab-renderer`，保留现有学生端操作及准入规则。控制器、提交队列、维护队列、宿主能力与服务契约不变。

## 页面与准入

| 页面          | 入口与状态                                                           | 操作                                 | 数据与异常                                                                 |
| ------------- | -------------------------------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------- |
| 激活          | 启动完成且未激活                                                     | 输入激活码、激活、刷新连接、窗口控制 | `controller.activate`；失败使用共享 Banner；执行中禁用重复操作             |
| 待机          | 存储异常、未绑定、版本异常、设备停用、服务不可用、维护或维护锁下失联 | 现有刷新连接与窗口控制               | 展示控制器状态、设备信息和部署测试进度；不提供业务导航                     |
| 试卷          | ready；正常模式失联时仍可进入但不显示练习入口                        | 开始练习                             | `controller.prepare`；下载与校验保持原流程；空、错误及准备中状态由页面呈现 |
| 处理中        | ready 或无维护/版本锁的普通失联                                      | 选择、导出、手动重试                 | 未完成记录；断网禁止重试，允许本地导出                                     |
| 异常          | 同处理中                                                             | 选择、导出、手动重试                 | 原有错误筛选；`receipt-only` 不提供重传                                    |
| 历史          | 同处理中                                                             | 选择、导出                           | 已完成记录；无本地归档者不可选；无删除操作                                 |
| 练习/部署测试 | 原控制器准入                                                         | 原播放器操作                         | 播放器保持独立沉浸视图；断网/维护不中断已开始练习；状态提示位于播放器之上  |

## UI 落点

- 共享 bootstrap 与 Vite 启动占位、AppShell/TitleBar/Sidebar、MemoryRouter 与路由注册表。
- 激活、待机、试卷、记录独立页面；共享 Button/Field/Banner/EmptyState/IconButton。
- CSS Modules 使用现有设计令牌；不修改主程序共享组件的样式或行为。
- 使用共享 `useLabAction` 串行用户操作，`useSelection` 管理选择；切页清空选择，后台更新后仅导出当前列表中仍有归档的记录。
- 学生入口开启宿主现有 `frameless` 选项，沿用教师端 Windows hidden title bar 方案。
- 原非 UI 控制器及队列文件保留路径，避免无关业务迁移。

## 验证

- 学生单测包含门控、断网导出、切页及后台记录变化、练习期间状态变化。
- Electron 原学生流程保留能力隔离、保存期间关闭保护、练习与提交、部署测试及清理断言。
- 原“历史”按钮断言改为共享侧栏链接；新增导航、选择、窄窗口、启动与关闭断言及截图。
- 主程序默认烟雾测试与完整 lab Electron 集成测试。

2026-09-21 验证结果：

- `yarn tsc --noEmit -p apps/lab-student/tsconfig.json` 通过；修改范围 ESLint 通过。
- 学生端单测 31 项通过；全仓 `yarn test:vitest` 1388 项通过、7 项跳过。
- `xvfb-run -a yarn lab:test:integration` 全部 5 项通过；截图稳定化后学生完整流程再次通过。
- `xvfb-run -a yarn test:smoke` 12 项通过。
- 已检查 1280px 常规窗口和 760px 窄窗口截图，以及部署测试播放器的通知层。截图位于 `test-results/lab/`。
- Windows 原生边框、拖动和缩放保留人工验收；没有改动共享 UI 组件或主程序业务。

## 后续必须修复：学生端维护待机页

- [ ] 按[部署设计第 8.2 节](docs/lab-deployment-design.md#82-维护待机画面)补齐并验收维护待机页。该页用于管理员巡视整间机房，属于必须完成项，不能以当前已有的状态文案视为完成。
- [ ] 展示配置的服务名称；它不同于产品名“曹二听说101”。
- [ ] 明确展示学生端激活状态和最近一次成功同步时间。
- [ ] 将软件图标放大到远处可识别；标题栏当前的 23px 图标不满足要求。设备编号也必须从远处容易识别。
- [ ] 增大维护页文字字号，尤其是服务名称、设备编号、维护/连接状态等巡检关键信息；按全屏远距离查看场景核对可读性。
- [ ] 保留并清晰展示设备编号、计算机名、机房或座位、客户端版本、维护模式状态和管理服务器连接状态。
- [ ] 验收失联、版本异常或激活失效时显示对应异常状态，不残留“已连接正常”等正常维护标识。
