# Smoke data

This directory preserves the early smoke-test inputs and outputs copied from
`textpa/artifacts/smoke` on 2026-08-09. It contains the acoustic cues,
transcripts, their manifests, and the paper-model finalized outputs used while
checking the pipeline.
