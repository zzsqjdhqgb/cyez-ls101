# Finalized benchmark data

These are the derived `*-final.jsonl` files copied verbatim from
`textpa/artifacts/llm-benchmark` on 2026-08-09. Their raw model responses and
manifests are in `../llm-benchmark/`.

They are kept because the test environment may be destroyed. They can also be
regenerated with `textpa finalize` from the raw JSONL files.
