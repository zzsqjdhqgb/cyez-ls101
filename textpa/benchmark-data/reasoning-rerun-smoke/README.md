# Reasoning rerun smoke data

This directory preserves the single-sample reasoning-depth reruns copied from
`textpa/artifacts/reasoning-rerun-smoke` on 2026-08-09. It contains the shared
cue input, raw JSONL responses, and manifests for DeepSeek v4 Flash max, Sol
high, and Luna max.

The Luna request did not produce a usable response; its captured raw record is
retained to document the failed attempt.
